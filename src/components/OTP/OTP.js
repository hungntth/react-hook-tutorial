import React from 'react';
import PropTypes from 'prop-types';
import GenerateOTP from './GenerateOTP';

OTP.propTypes = {

};

function OTP(props) {
    return (
        <div>
            <GenerateOTP />
        </div>
    );
}

export default OTP;